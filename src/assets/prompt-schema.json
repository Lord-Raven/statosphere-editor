{
  "title": "Prompts",
  "description": "An array of additional prompts to include under specified conditions.",
  "type": "array",
  "minItems": 0,
  "uniqueItems": true,
  "items": {
    "type": "object",
    "required": [
      "condition",
      "prompt"
    ],
    "properties": {
      "condition": {
        "title": "Condition",
        "description": "Requirement for inclusion.",
        "type": "string",
        "minLength": 1
      },
      "prompt": {
        "title": "Prompt",
        "description": "Prompt to include.",
        "type": "string",
        "minLength": 1
      }
    }
  }
}